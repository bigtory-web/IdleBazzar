# 바자르 SD 유닛 생성기

`start-character-generator.ps1`을 PowerShell에서 실행한 뒤 브라우저로 `http://127.0.0.1:4319`를 엽니다.

## 생성 규칙

- 캐릭터: 아이들, 공격
- 몬스터·보스: 이동, 공격, 사망
- 첫 결과를 다음 상태의 참고 이미지로 사용해 디자인 일관성을 높입니다.
- 생성 중에는 **생성 취소** 버튼을 사용할 수 있으며, 한 상태가 3분을 넘으면 자동 중단됩니다.
- 원본은 `assets/generated/<생성 시각-이름>/`에 1024px PNG로 보관됩니다.
- 화면의 저장 버튼은 게임 투입용 64px 또는 128px PNG로 축소합니다.

## API 키

둘 중 하나를 사용합니다.

1. 생성기 화면의 **API 키 설정**에 입력합니다. 브라우저나 파일에 저장하지 않습니다.
2. 실행 전 PowerShell에서 `$env:OPENAI_API_KEY='키'`를 설정합니다.

이미지는 OpenAI `gpt-image-2`로 생성되며 API 사용료가 발생할 수 있습니다.
