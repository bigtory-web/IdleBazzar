# IdleBazzar 개발 인수인계

마지막 정리: 2026-07-26

이 문서는 현재 작업을 다른 개발자가 바로 이어받을 수 있도록 실제 코드 상태, 최근 변경, 확인된 문제와 다음 작업 순서를 정리한 운영 인수인계 문서다. 전투 규칙의 상세 기준은 `GAME_DESIGN_HANDOFF.md`를 함께 참고한다.

## 1. 저장소와 실행

- 로컬 경로: `C:\Users\kogus\OneDrive\130.프로젝트\game project\IdleBazzar`
- GitHub: `https://github.com/bigtory-web/IdleBazzar.git`
- 기본 브랜치: `main`
- 기술 구성: 별도 빌드 과정이 없는 HTML/CSS/JavaScript + Canvas

PowerShell에서 아래 명령으로 실행한다.

```powershell
.\serve.ps1
```

기본 주소는 `http://127.0.0.1:4173/`이다. 포트를 바꾸려면 `.\serve.ps1 -Port 4174`처럼 실행한다.

## 2. 가장 중요한 현재 상태

### 의도한 테스트 범위

사용자가 원하는 현재 테스트 캐릭터는 아래 4종이다.

| 캐릭터 | 획득 방식 | 실제 기본 효과 |
|---|---|---|
| 단검 | 시작 시 기본 지급 | 직선탄, 타격 6, 쿨타임 4.8초 |
| 화염검 | 첫 상점 | 유도탄, 타격 5, 화염 1, 쿨타임 5.8초 |
| 화염총 | 첫 상점 | 총·유도탄, 타격 8, 화염 2, 쿨타임 5.8초, 탄약 2 |
| 보급 정령 | 첫 상점 | 사역마·유도탄, 타격 2, 바람 1초, 쿨타임 5.5초, 인접 총 1발 장전 |

현재 실제로 시험하려는 전투 요소는 타격, 화염, 바람 가속, 총·탄약, 인접 장전, 직선탄과 유도탄이다.

### 코드와 요구사항의 불일치

`card-data.js`에는 향후 후보를 포함한 23종이 등록되어 있고 `rollShop()`은 이 전체 데이터를 해금 대상으로 사용한다. 따라서 현재 코드는 4종 테스트 제한이 완전히 적용되지 않았다.

- 게임 시작: 단검 기본 지급, 첫 상점은 화염검·화염총·보급 정령으로 고정
- 첫 상점 새로고침: S 크기 8종 등장 가능
- 웨이브 1 이후: S·M 크기 17종 등장 가능
- 웨이브 4 이후: S·M·L 전체 23종 등장 가능

다음 개발자가 가장 먼저 처리할 문제다. 테스트용 ID 목록을 별도 상수로 만들고 `rollShop()`이 그 목록만 사용하게 제한해야 한다. 후보 23종을 삭제할지는 사용자가 아직 결정하지 않았다.

## 3. 최근 UI 변경

### 카드 호버 정보

- 호버 창을 기존보다 좁고 세로로 길게 변경했다.
- 공통 수치는 설명문에서 빼고 하스스톤 카드처럼 모서리 배지에 아이콘과 숫자로 표시한다.
  - 왼쪽 위: 쿨타임
  - 오른쪽 위: 탄약
  - 왼쪽 아래: 타격
  - 오른쪽 아래: 화염
- 값이 없는 타격·화염 배지는 표시하지 않는다.
- 총이 아닌 카드는 탄약 배지를 표시하지 않는다.
- `유도탄`과 `직선탄`은 `총`, `무기`, `사역마`, 크기와 같은 테두리 태그 행에 표시한다.
- 태그 앞의 `#`은 사용하지 않는다.
- 화염의 `5초·재적용 갱신`, 총의 공통 탄약 규칙처럼 모든 카드에 반복되는 설명은 호버에서 제거했다.
- 카드 설명은 평서문 대신 짧은 효과 문구로 정리했다.

주의: `화염총`의 `강한 화염탄`, `빙결총`의 `강한 냉기탄`은 별도 전용 로직이 없는 설명 문구다. 사용자는 이 점을 이미 문제로 인식했다. 고유 효과가 없는 카드의 중앙 설명을 숨기거나 실제 고유 효과를 설계해야 한다.

### 창고와 HUD

- 창고 영역의 표시 높이를 전투 보드와 비슷하게 맞췄다.
- 판매 휴지통 버튼을 창고 우측에서 화면 상단 골드 버튼 옆으로 이동했다.
- 골드와 판매 버튼의 크기·정렬·표현을 통일했다.

## 4. 전투 구현 핵심

- 플레이어 보드는 10칸이며 S/M/L은 각각 1/2/3칸을 사용한다.
- 직선탄은 고정 궤적이다. 유도탄은 지정된 목표를 추적하며, 목표가 죽으면 다른 적을 찾지 않고 사망 위치까지 이동한 뒤 소멸한다.
- 화염은 5초 동안 유지되며 0.75초마다 스택만큼 피해를 준다.
- 냉기는 2.4초 동안 스택당 이동·공격을 12% 늦추며 최대 4스택, 최대 45%다.
- 바람은 효과 시간 동안 쿨타임 감소 속도를 2배로 만든다.
- 보호막은 최대 45이고 초당 3 감소한다.
- 산탄의 남는 탄환은 주 대상에게 55% 위력으로 적용된다.
- 폭발은 주변 적에게 직접 피해의 60%를 준다.
- 다중시전의 추가 시전은 70% 위력이다.
- 총 탄약은 S/M/L 각각 2/3/4이며 웨이브 시작 시 최대치로 충전된다.
- `reload: 1`인 사역마는 인접 총 중 탄약이 가장 적은 한 장에 1발을 장전한다.

현재 카드별 전용 분기 로직은 거의 없고, 대부분 위 공통 효과의 조합으로 동작한다.

## 5. 캐릭터 이미지와 도구 상태

- `resources/CHARACTER_IMAGE_GUIDE.md`에 직업별 의상·실루엣 분리와 얼굴 표현 기준을 추가했다.
- 총잡이의 portrait/idle/attack 이미지가 갱신되었다.
- 개구리 궁수, 방패병, 마법사의 portrait 이미지가 추가되었다.
- 기존 `resources/characters/wizard` 자료는 유지한다.
- 별도 character-generator HTML/JS/CSS/서버/실행 스크립트는 사용자 요청으로 제거했다.
- `assets/generated/wizard`의 생성 결과 4종도 character-generator 정리 범위로 제거했다.

## 6. 주요 파일

| 파일 | 역할 |
|---|---|
| `index.html` | 게임 DOM, HUD, 상점, 창고 구조 |
| `styles.css` | 10:16 레이아웃, 카드·호버·HUD·창고 스타일 |
| `game.js` | 상태, 상점, 배치, 전투, 탄환, 효과, 호버 렌더링 |
| `card-data.js` | 플레이어 후보 23종과 몬스터 기본 데이터 |
| `GAME_DESIGN_HANDOFF.md` | 확정 전투 규칙과 기획 기준 |
| `resources/CHARACTER_IMAGE_GUIDE.md` | 캐릭터 이미지 제작 규칙 |
| `serve.ps1` | 로컬 정적 서버 |

호버 관련 핵심 함수는 `game.js`의 `detailMetaHtml()`, `detailStatsHtml()`, `detailEffectHtml()`, `showShopPopover()`, `showUnitPopover()`이다. 상점 카드 제한은 `rollShop()`에서 처리한다.

## 7. 검증 상태

- 번들 Node.js로 `game.js`, `card-data.js` 문법 검사를 통과했다.
- `git diff --check`를 통과했다.
- 로컬 브라우저에서 화염총과 보급 정령 호버를 확인했다.
- 화염총 호버에는 `S / 총 / 유도탄` 태그와 쿨타임·탄약·타격·화염 모서리 배지가 표시된다.
- 보급 정령 호버에서 반복 규칙 문장이 제거되고 바람 수치와 고유 설명만 표시되는 것을 확인했다.

## 8. 다음 작업 우선순위

1. 상점 카드 풀을 단검·화염검·화염총·보급 정령의 4종 테스트 범위로 제한한다.
2. 단검은 시작 지급 전용으로 둘지 상점에도 나오게 할지 사용자에게 확인한다.
3. 고유 효과가 없는 카드의 중앙 설명을 완전히 숨길지, 실제 전용 효과를 추가할지 결정한다.
4. 테스트 4종만으로 탄약 2발과 보급 정령 장전의 전투 리듬을 조정한다.
5. 테스트 범위가 확정된 뒤 후보 23종 데이터를 유지할지 별도 보관할지 정리한다.
6. 캐릭터 이미지를 실제 전투 렌더러에 연결할 때 기존 몬스터·캐릭터 자산을 임의로 덮어쓰지 않는다.

## 9. 작업 시 주의

- 프로젝트의 공식 작업 경로는 이 저장소 경로다. 과거 폴더인 `C:\Users\kogus\OneDrive\문서\바자르 키우기`는 수정하지 않는다.
- 사용자 이미지 변경을 임의로 되돌리거나 교체하지 않는다.
- 요청 없이 새로운 전투 효과를 8개 공통 효과 밖에 추가하지 않는다.
- GitHub에 푸시하는 작업은 사용자가 별도로 요청할 때만 진행한다.
